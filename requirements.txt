# KEPCO RPA Dependencies

# Core dependencies
websockets>=12.0
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0
lxml>=4.9.0
PySide6>=6.6.0
holidays>=0.101       # 한국 공휴일(대체공휴일 포함) 오프라인 계산 — "내일(주말·공휴일 제외)" 모드

# Optional (for legacy Excel format support)
xlrd>=2.0.0

# 개발/테스트
pytest>=8.0.0

# Note: Standard library modules used (no install needed)
# - asyncio
# - os
# - sys
# - datetime
# - uuid
# - io
