# 단일 EXE 재설계 — Plan 1: 코어 재구성 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 콘솔 스크립트를 3계층 패키지로 재구성하고, UI 없이도 완결·테스트 가능한 순수 코어(프리셋·규칙 엔진·엑셀 생성·본부 매핑)와 어댑터 계층을 만든다.

**Architecture:** `app/adapters/`(기존 auth/downloader/mailer/config 이동·재사용), `app/core/`(신규 순수 로직: departments·settings·preset_store·engine·excel_writer). 코어는 사내망·UI에 의존하지 않는 순수 함수로 pytest 단위 테스트 가능. UI(Plan 2)가 이 인터페이스 위에 올라간다.

**Tech Stack:** Python 3.10+, pandas, openpyxl, pytest. (PySide6는 Plan 2에서 추가)

관련 스펙: `docs/superpowers/specs/2026-07-02-단일exe-대화형-재설계-design.md`

## Global Constraints

- 모든 UI 텍스트·주석·문서는 한국어.
- 코어(`app/core/`)는 네트워크·Qt·파일경로 하드코딩에 의존하지 않는다. 출력 경로 등 외부 자원은 인자로 주입받는다.
- 규칙·필터·시트분리 열은 **열 문자(A,B…)가 아닌 실제 헤더명 문자열**로 매칭한다.
- 우선순위 라벨은 정확히 `"1순위"`, `"2순위"`, `"3순위"` 문자열을 사용한다.
- 파이썬 패키지 임포트는 절대경로(`from app.core.settings import ...`)를 사용한다. 테스트는 저장소 루트에서 `pytest` 실행.
- 커밋 메시지 말미에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 포함.

---

### Task 1: 패키지 구조 생성 + 어댑터 이동

기존 `auth.py`, `downloader.py`, `mailer.py`, `config.py`를 `app/adapters/`로 옮기고 임포트를 패키지 절대경로로 수정한다. 코어/테스트 디렉터리의 빈 패키지도 만든다.

**Files:**
- Create: `app/__init__.py`, `app/core/__init__.py`, `app/adapters/__init__.py`, `tests/__init__.py`, `conftest.py`
- Move: `auth.py`→`app/adapters/auth.py`, `downloader.py`→`app/adapters/downloader.py`, `mailer.py`→`app/adapters/mailer.py`, `config.py`→`app/adapters/config.py`
- Modify: 위 3개 어댑터의 `from config import ...` → `from app.adapters.config import ...`

**Interfaces:**
- Produces: `app.adapters.config` 모듈이 상수 `WORK_MONITOR_URL`, `BIZMAIL_URL`, `POWERGATE_WS_URI`, `DEPARTMENT_CODE`, `LIST_COUNT`, `PAGE`, `WEBSOCKET_TIMEOUT`, `HTTP_TIMEOUT`와 함수 `get_base_dir()`, `get_output_dir()`를 노출(기존 그대로). `get_classification_file_path()`는 삭제.
- Produces: `app.adapters.auth.authenticate()`, `app.adapters.downloader.download_excel_to_dataframe(...)`, `app.adapters.mailer.send_bizmail(...)` (시그니처 변경 없음).

- [ ] **Step 1: 디렉터리와 빈 패키지 파일 생성**

```bash
mkdir -p app/core app/adapters tests
touch app/__init__.py app/core/__init__.py app/adapters/__init__.py tests/__init__.py
```

- [ ] **Step 2: 어댑터 파일을 git mv로 이동**

```bash
git mv auth.py app/adapters/auth.py
git mv downloader.py app/adapters/downloader.py
git mv mailer.py app/adapters/mailer.py
git mv config.py app/adapters/config.py
```

- [ ] **Step 3: config.py에서 분류표 경로 함수 제거**

`app/adapters/config.py`에서 아래 함수를 삭제(더 이상 분류표.xlsx를 쓰지 않음):

```python
def get_classification_file_path():
    """Get the path to 분류표.xlsx"""
    return os.path.join(get_base_dir(), '분류표.xlsx')
```

- [ ] **Step 4: 어댑터 임포트를 패키지 절대경로로 수정**

`app/adapters/auth.py`:
```python
from app.adapters.config import POWERGATE_WS_URI, WEBSOCKET_TIMEOUT
```
`app/adapters/downloader.py`:
```python
from app.adapters.config import (
    WORK_MONITOR_URL, HTTP_TIMEOUT,
    PAGE, LIST_COUNT, DEPARTMENT_CODE
)
```
`app/adapters/mailer.py`:
```python
from app.adapters.config import BIZMAIL_URL, HTTP_TIMEOUT
```

- [ ] **Step 5: conftest.py로 저장소 루트를 sys.path에 추가**

`conftest.py` (저장소 루트):
```python
import os
import sys

# pytest가 저장소 루트에서 app 패키지를 임포트할 수 있게 함
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
```

- [ ] **Step 6: 임포트 스모크 검증**

Run: `python -c "import app.adapters.auth, app.adapters.downloader, app.adapters.mailer, app.adapters.config; print('ok')"`
Expected: `ok` (ImportError 없이 출력)

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor: 어댑터(auth/downloader/mailer/config)를 app/adapters 패키지로 이동

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 본부 매핑 (departments.py)

29개 본부↔부서코드를 상수로 고정하고 조회·그룹 함수를 제공한다.

**Files:**
- Create: `app/core/departments.py`
- Test: `tests/test_departments.py`

**Interfaces:**
- Produces:
  - `Department` (frozen dataclass): `.code: str`, `.name: str`, `.group: str`
  - `DEPARTMENTS: list[Department]` (29개)
  - `all_departments() -> list[Department]`
  - `name_for_code(code: str) -> str | None`
  - `code_for_name(name: str) -> str | None`
  - `grouped() -> dict[str, list[Department]]` (키 순서: `"지역본부"`, `"건설본부"`, `"본사"`)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_departments.py`:
```python
from app.core import departments as dep


def test_has_29_departments():
    assert len(dep.all_departments()) == 29


def test_name_for_code():
    assert dep.name_for_code("4200") == "강원본부"
    assert dep.name_for_code("6220") == "경인건설본부"
    assert dep.name_for_code("9999") is None


def test_code_for_name():
    assert dep.code_for_name("경남본부") == "5700"
    assert dep.code_for_name("없는본부") is None


def test_grouped_keys_and_counts():
    g = dep.grouped()
    assert list(g.keys()) == ["지역본부", "건설본부", "본사"]
    assert len(g["지역본부"]) == 15
    assert len(g["건설본부"]) == 4
    assert len(g["본사"]) == 10


def test_all_codes_unique():
    codes = [d.code for d in dep.all_departments()]
    assert len(codes) == len(set(codes))
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_departments.py -v`
Expected: FAIL (`ModuleNotFoundError: app.core.departments`)

- [ ] **Step 3: 구현 작성**

`app/core/departments.py`:
```python
"""전국 본부↔부서코드 매핑 (하드코딩 고정 목록)"""

from dataclasses import dataclass


@dataclass(frozen=True)
class Department:
    code: str
    name: str
    group: str  # "지역본부" | "건설본부" | "본사"


DEPARTMENTS: list[Department] = [
    # 지역본부 (15)
    Department("3970", "서울본부", "지역본부"),
    Department("3800", "남서울본부", "지역본부"),
    Department("4000", "인천본부", "지역본부"),
    Department("3900", "경기본부", "지역본부"),
    Department("3420", "경기북부본부", "지역본부"),
    Department("4200", "강원본부", "지역본부"),
    Department("4500", "충북본부", "지역본부"),
    Department("4600", "대전세종충남본부", "지역본부"),
    Department("4800", "전북본부", "지역본부"),
    Department("5000", "광주전남본부", "지역본부"),
    Department("5200", "대구본부", "지역본부"),
    Department("5250", "경북본부", "지역본부"),
    Department("5500", "부산울산본부", "지역본부"),
    Department("5700", "경남본부", "지역본부"),
    Department("5900", "제주본부", "지역본부"),
    # 건설본부 (4)
    Department("6220", "경인건설본부", "건설본부"),
    Department("6486", "중부건설본부", "건설본부"),
    Department("6250", "남부건설본부", "건설본부"),
    Department("0147", "HVDC건설본부", "건설본부"),
    # 본사 (10)
    Department("0459", "기획부사장", "본사"),
    Department("0499", "전력혁신본부", "본사"),
    Department("0469", "경영관리본부", "본사"),
    Department("0479", "상생협력본부", "본사"),
    Department("0485", "안전&영업배전부사장", "본사"),
    Department("0464", "영업본부", "본사"),
    Department("0483", "기술혁신본부", "본사"),
    Department("0481", "전력계통본부", "본사"),
    Department("0905", "해외원전사업본부", "본사"),
    Department("0903", "원전수출본부", "본사"),
]


def all_departments() -> list[Department]:
    return list(DEPARTMENTS)


def name_for_code(code: str) -> str | None:
    for d in DEPARTMENTS:
        if d.code == code:
            return d.name
    return None


def code_for_name(name: str) -> str | None:
    for d in DEPARTMENTS:
        if d.name == name:
            return d.code
    return None


def grouped() -> dict[str, list[Department]]:
    result: dict[str, list[Department]] = {"지역본부": [], "건설본부": [], "본사": []}
    for d in DEPARTMENTS:
        result[d.group].append(d)
    return result
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_departments.py -v`
Expected: PASS (5개 모두)

- [ ] **Step 5: Commit**

```bash
git add app/core/departments.py tests/test_departments.py
git commit -m "feat: 본부 29개 부서코드 매핑(departments) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 프리셋 데이터 모델 (settings.py)

Rule/Filter/Preset 데이터클래스와 JSON 직렬화(`to_dict`/`from_dict`)를 정의한다.

**Files:**
- Create: `app/core/settings.py`
- Test: `tests/test_settings.py`

**Interfaces:**
- Produces:
  - `Rule(column:str, keyword:str, match:str="contains", priority:int=3, color:str="#FFFFFF")`
  - `Filter(column:str, op:str, value:str="")`  — op ∈ {"not_null","equals","contains","not_equals"}
  - `Preset(name:str, department_code:str, default_date_offset:int=1, drop_columns:list[str]=[], sheet_split_column:str="", rules:list[Rule]=[], filters:list[Filter]=[], sort:str="none", mail_from_name:str="", mail_from_email:str="", mail_recipients:list[str]=[], mail_subject:str="", mail_body:str="")`
  - `Preset.to_dict() -> dict`, `Preset.from_dict(d:dict) -> Preset` (중첩 Rule/Filter 포함)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_settings.py`:
```python
from app.core.settings import Rule, Filter, Preset


def test_defaults():
    p = Preset(name="강원", department_code="4200")
    assert p.default_date_offset == 1
    assert p.sort == "none"
    assert p.rules == []
    assert p.drop_columns == []


def test_roundtrip_serialization():
    p = Preset(
        name="강원본부 기본",
        department_code="4200",
        default_date_offset=0,
        drop_columns=["담당자", "장비"],
        sheet_split_column="지사",
        rules=[Rule(column="공사명", keyword="활선", match="contains", priority=1, color="#FFFF00")],
        filters=[Filter(column="지사", op="not_null")],
        sort="priority",
        mail_from_name="홍길동",
        mail_from_email="hong@kepco.co.kr",
        mail_recipients=["a@kepco.co.kr", "b@kepco.co.kr"],
        mail_subject="{DATE} 점검 리스트",
        mail_body="본문 {DATE}",
    )
    d = p.to_dict()
    p2 = Preset.from_dict(d)
    assert p2 == p
    assert isinstance(p2.rules[0], Rule)
    assert isinstance(p2.filters[0], Filter)


def test_from_dict_tolerates_missing_optionals():
    p = Preset.from_dict({"name": "x", "department_code": "4200"})
    assert p.name == "x"
    assert p.rules == []
    assert p.mail_recipients == []
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_settings.py -v`
Expected: FAIL (`ModuleNotFoundError: app.core.settings`)

- [ ] **Step 3: 구현 작성**

`app/core/settings.py`:
```python
"""프리셋 데이터 모델 — 모든 사용자 설정을 담는 직렬화 가능한 구조"""

from dataclasses import dataclass, field, asdict


@dataclass
class Rule:
    column: str
    keyword: str
    match: str = "contains"   # "contains" | "equals"
    priority: int = 3         # 1 | 2 | 3
    color: str = "#FFFFFF"    # 해당 순위 행 배경색 (헥사)


@dataclass
class Filter:
    column: str
    op: str                   # "not_null" | "equals" | "contains" | "not_equals"
    value: str = ""           # not_null이면 무시


@dataclass
class Preset:
    name: str
    department_code: str
    default_date_offset: int = 1        # 0=오늘, 1=내일 …
    drop_columns: list[str] = field(default_factory=list)
    sheet_split_column: str = ""        # 지사 시트 분리 기준 열(헤더명)
    rules: list[Rule] = field(default_factory=list)
    filters: list[Filter] = field(default_factory=list)
    sort: str = "none"                  # "none" | "priority" | "<열이름>"
    mail_from_name: str = ""
    mail_from_email: str = ""
    mail_recipients: list[str] = field(default_factory=list)
    mail_subject: str = ""
    mail_body: str = ""

    def to_dict(self) -> dict:
        return asdict(self)

    @classmethod
    def from_dict(cls, d: dict) -> "Preset":
        data = dict(d)
        data["rules"] = [Rule(**r) for r in data.get("rules", [])]
        data["filters"] = [Filter(**f) for f in data.get("filters", [])]
        # 알 수 없는 키는 무시하고 정의된 필드만 사용
        known = cls.__dataclass_fields__.keys()
        filtered = {k: v for k, v in data.items() if k in known}
        return cls(**filtered)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_settings.py -v`
Expected: PASS (3개)

- [ ] **Step 5: Commit**

```bash
git add app/core/settings.py tests/test_settings.py
git commit -m "feat: 프리셋 데이터 모델(Rule/Filter/Preset) + JSON 직렬화 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 프리셋 저장소 (preset_store.py)

프리셋을 지정 디렉터리의 `<이름>.json`으로 저장·로드·삭제·목록화한다. 테스트 가능하도록 디렉터리를 인자로 받는다.

**Files:**
- Create: `app/core/preset_store.py`
- Test: `tests/test_preset_store.py`

**Interfaces:**
- Consumes: `app.core.settings.Preset`
- Produces:
  - `list_presets(directory: Path) -> list[str]` (이름 정렬)
  - `save_preset(preset: Preset, directory: Path) -> Path`
  - `load_preset(name: str, directory: Path) -> Preset`
  - `delete_preset(name: str, directory: Path) -> None`
  - `_safe_filename(name: str) -> str` (내부, 파일명 안전화)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_preset_store.py`:
```python
from app.core import preset_store as ps
from app.core.settings import Preset, Rule


def test_save_and_load(tmp_path):
    p = Preset(name="강원본부 기본", department_code="4200",
               rules=[Rule(column="공사명", keyword="활선", priority=1, color="#FFFF00")])
    path = ps.save_preset(p, tmp_path)
    assert path.exists()
    loaded = ps.load_preset("강원본부 기본", tmp_path)
    assert loaded == p


def test_list_presets_sorted(tmp_path):
    ps.save_preset(Preset(name="나본부", department_code="1"), tmp_path)
    ps.save_preset(Preset(name="가본부", department_code="2"), tmp_path)
    assert ps.list_presets(tmp_path) == ["가본부", "나본부"]


def test_delete_preset(tmp_path):
    ps.save_preset(Preset(name="삭제대상", department_code="1"), tmp_path)
    ps.delete_preset("삭제대상", tmp_path)
    assert ps.list_presets(tmp_path) == []


def test_list_presets_empty_dir(tmp_path):
    assert ps.list_presets(tmp_path) == []
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_preset_store.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: 구현 작성**

`app/core/preset_store.py`:
```python
"""프리셋 JSON 저장소 — presets/<이름>.json 로드·저장·삭제·목록"""

import json
import re
from pathlib import Path

from app.core.settings import Preset


def _safe_filename(name: str) -> str:
    """파일명에 쓸 수 없는 문자를 _로 치환"""
    return re.sub(r'[\\/:*?"<>|]', "_", name).strip()


def save_preset(preset: Preset, directory: Path) -> Path:
    directory = Path(directory)
    directory.mkdir(parents=True, exist_ok=True)
    path = directory / f"{_safe_filename(preset.name)}.json"
    with open(path, "w", encoding="utf-8") as f:
        json.dump(preset.to_dict(), f, ensure_ascii=False, indent=2)
    return path


def load_preset(name: str, directory: Path) -> Preset:
    path = Path(directory) / f"{_safe_filename(name)}.json"
    with open(path, "r", encoding="utf-8") as f:
        return Preset.from_dict(json.load(f))


def list_presets(directory: Path) -> list[str]:
    directory = Path(directory)
    if not directory.exists():
        return []
    names = []
    for p in directory.glob("*.json"):
        try:
            with open(p, "r", encoding="utf-8") as f:
                names.append(json.load(f).get("name", p.stem))
        except (json.JSONDecodeError, OSError):
            continue
    return sorted(names)


def delete_preset(name: str, directory: Path) -> None:
    path = Path(directory) / f"{_safe_filename(name)}.json"
    if path.exists():
        path.unlink()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_preset_store.py -v`
Expected: PASS (4개)

- [ ] **Step 5: Commit**

```bash
git add app/core/preset_store.py tests/test_preset_store.py
git commit -m "feat: 프리셋 JSON 저장소(preset_store) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 규칙 엔진 — 필터 + drop (engine.py 1부)

원본 DataFrame에 필터(AND)와 컬럼 drop을 적용하는 순수 함수를 만든다.

**Files:**
- Create: `app/core/engine.py`
- Test: `tests/test_engine_filters.py`

**Interfaces:**
- Consumes: `app.core.settings.Filter`
- Produces:
  - `apply_filters(df: pd.DataFrame, filters: list[Filter]) -> pd.DataFrame`
  - `apply_drop(df: pd.DataFrame, drop_columns: list[str]) -> pd.DataFrame`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_engine_filters.py`:
```python
import pandas as pd
from app.core.engine import apply_filters, apply_drop
from app.core.settings import Filter


def _df():
    return pd.DataFrame({
        "지사": ["강릉", None, "원주"],
        "공사명": ["활선 작업", "일반 점검", "변전 점검"],
        "상태": ["예정", "취소", "예정"],
    })


def test_not_null_filter_drops_null_rows():
    out = apply_filters(_df(), [Filter(column="지사", op="not_null")])
    assert list(out["지사"]) == ["강릉", "원주"]


def test_equals_filter():
    out = apply_filters(_df(), [Filter(column="상태", op="equals", value="예정")])
    assert len(out) == 2
    assert set(out["상태"]) == {"예정"}


def test_contains_and_not_equals_combined_as_and():
    out = apply_filters(_df(), [
        Filter(column="공사명", op="contains", value="점검"),
        Filter(column="상태", op="not_equals", value="취소"),
    ])
    assert list(out["공사명"]) == ["변전 점검"]


def test_filter_on_missing_column_is_ignored():
    out = apply_filters(_df(), [Filter(column="없는열", op="not_null")])
    assert len(out) == 3


def test_apply_drop_removes_named_columns_only():
    out = apply_drop(_df(), ["상태", "없는열"])
    assert list(out.columns) == ["지사", "공사명"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_engine_filters.py -v`
Expected: FAIL (`ModuleNotFoundError: app.core.engine`)

- [ ] **Step 3: 구현 작성**

`app/core/engine.py`:
```python
"""규칙 엔진 — 원본 DataFrame + Preset → 가공된 DataFrame (순수 함수)"""

import pandas as pd

from app.core.settings import Preset, Rule, Filter

PRIORITY_LABELS = {1: "1순위", 2: "2순위", 3: "3순위"}


def apply_filters(df: pd.DataFrame, filters: list[Filter]) -> pd.DataFrame:
    """필터를 AND로 결합해 행을 선별. 존재하지 않는 열 필터는 무시."""
    result = df
    for f in filters:
        if f.column not in result.columns:
            continue
        col = result[f.column]
        if f.op == "not_null":
            mask = col.notna() & (col.astype(str).str.strip() != "")
        elif f.op == "equals":
            mask = col.astype(str) == f.value
        elif f.op == "not_equals":
            mask = col.astype(str) != f.value
        elif f.op == "contains":
            mask = col.astype(str).str.contains(f.value, regex=False, na=False)
        else:
            continue
        result = result[mask]
    return result


def apply_drop(df: pd.DataFrame, drop_columns: list[str]) -> pd.DataFrame:
    """지정된 이름의 열만 제거. 존재하지 않는 이름은 무시."""
    return df.drop(columns=drop_columns, errors="ignore")
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_engine_filters.py -v`
Expected: PASS (5개)

- [ ] **Step 5: Commit**

```bash
git add app/core/engine.py tests/test_engine_filters.py
git commit -m "feat: 규칙 엔진 필터·drop 로직 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 규칙 엔진 — 우선순위 + 정렬 + process (engine.py 2부)

우선순위 규칙 적용, 정렬, 전체 파이프라인(`process`)을 추가한다.

**Files:**
- Modify: `app/core/engine.py`
- Test: `tests/test_engine_priority.py`

**Interfaces:**
- Consumes: `app.core.settings.Preset`, `Rule`; Task 5의 `apply_filters`, `apply_drop`
- Produces:
  - `assign_priority(df, rules: list[Rule]) -> pd.DataFrame` — 열 `점검순위`(문자열 라벨)와 내부 열 `_row_color`(헥사 문자열, 매칭 없으면 "") 추가
  - `sort_df(df, sort: str) -> pd.DataFrame` — "none"이면 원순서 유지, "priority"면 1→2→3순위, 그 외는 해당 열 오름차순
  - `process(df, preset: Preset) -> pd.DataFrame` — 필터 → 우선순위 → drop → 정렬 순서로 실행

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_engine_priority.py`:
```python
import pandas as pd
from app.core.engine import assign_priority, sort_df, process
from app.core.settings import Preset, Rule, Filter


def _df():
    return pd.DataFrame({
        "지사": ["강릉", "원주", "속초"],
        "공사명": ["활선 작업", "변전 점검", "일반 보수"],
        "담당자": ["김", "이", "박"],
    })


def test_assign_priority_last_rule_wins_and_sets_color():
    rules = [
        Rule(column="공사명", keyword="점검", match="contains", priority=2, color="#F7B9AF"),
        Rule(column="공사명", keyword="활선", match="contains", priority=1, color="#FFFF00"),
    ]
    out = assign_priority(_df(), rules)
    assert list(out["점검순위"]) == ["1순위", "2순위", "3순위"]
    assert list(out["_row_color"]) == ["#FFFF00", "#F7B9AF", ""]


def test_assign_priority_equals_match():
    rules = [Rule(column="지사", keyword="원주", match="equals", priority=1, color="#FFFF00")]
    out = assign_priority(_df(), rules)
    assert list(out["점검순위"]) == ["3순위", "1순위", "3순위"]


def test_assign_priority_no_rules_all_third():
    out = assign_priority(_df(), [])
    assert list(out["점검순위"]) == ["3순위", "3순위", "3순위"]


def test_sort_by_priority():
    df = assign_priority(_df(), [
        Rule(column="공사명", keyword="활선", priority=1, color="#FFFF00"),
        Rule(column="공사명", keyword="변전", priority=2, color="#F7B9AF"),
    ])
    out = sort_df(df, "priority")
    assert list(out["점검순위"]) == ["1순위", "2순위", "3순위"]


def test_sort_none_keeps_order():
    df = assign_priority(_df(), [])
    out = sort_df(df, "none")
    assert list(out["지사"]) == ["강릉", "원주", "속초"]


def test_process_applies_filter_priority_drop_sort():
    preset = Preset(
        name="t", department_code="4200",
        filters=[Filter(column="담당자", op="not_null")],
        rules=[Rule(column="공사명", keyword="활선", priority=1, color="#FFFF00")],
        drop_columns=["담당자"],
        sort="priority",
    )
    out = process(_df(), preset)
    assert "담당자" not in out.columns          # drop 적용
    assert "점검순위" in out.columns
    assert out.iloc[0]["점검순위"] == "1순위"     # priority 정렬로 활선 행이 맨 위
    assert list(out.columns).count("_row_color") == 1
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_engine_priority.py -v`
Expected: FAIL (`ImportError: cannot import name 'assign_priority'`)

- [ ] **Step 3: 구현 추가**

`app/core/engine.py` 끝에 추가:
```python
def assign_priority(df: pd.DataFrame, rules: list[Rule]) -> pd.DataFrame:
    """각 행에 규칙을 순차 적용(뒤 규칙이 우선). 매칭 없으면 3순위.
    열 '점검순위'(라벨)와 내부 열 '_row_color'(헥사) 추가."""
    labels: list[str] = []
    colors: list[str] = []
    for _, row in df.iterrows():
        priority = 3
        color = ""
        for rule in rules:
            if rule.column not in df.columns:
                continue
            value = row[rule.column]
            value = "" if pd.isna(value) else str(value)
            if rule.match == "equals":
                hit = value == rule.keyword
            else:  # contains
                hit = rule.keyword in value
            if hit:
                priority = rule.priority
                color = rule.color
        labels.append(PRIORITY_LABELS.get(priority, "3순위"))
        colors.append(color)
    out = df.copy()
    out["점검순위"] = labels
    out["_row_color"] = colors
    return out


def sort_df(df: pd.DataFrame, sort: str) -> pd.DataFrame:
    """정렬. 'none'=원순서, 'priority'=1→2→3순위, 그 외=해당 열 오름차순."""
    if sort == "none" or not sort:
        return df
    if sort == "priority":
        order = {"1순위": 0, "2순위": 1, "3순위": 2}
        key = df["점검순위"].map(order).fillna(9)
        return df.assign(_ord=key).sort_values("_ord", kind="stable").drop(columns="_ord")
    if sort in df.columns:
        return df.sort_values(by=sort, kind="stable")
    return df


def process(df: pd.DataFrame, preset: Preset) -> pd.DataFrame:
    """전체 파이프라인: 필터 → 우선순위 → drop → 정렬.
    (우선순위·필터를 drop보다 먼저 실행해 drop된 열도 규칙에 사용 가능)"""
    out = apply_filters(df, preset.filters)
    out = assign_priority(out, preset.rules)
    out = apply_drop(out, preset.drop_columns)
    out = sort_df(out, preset.sort)
    return out.reset_index(drop=True)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_engine_priority.py -v`
Expected: PASS (6개)

- [ ] **Step 5: Commit**

```bash
git add app/core/engine.py tests/test_engine_priority.py
git commit -m "feat: 규칙 엔진 우선순위·정렬·process 파이프라인 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 엑셀 생성기 (excel_writer.py)

가공된 DataFrame을 지사별 다중 시트 + 서식(순위 행 색·헤더·테두리·틀고정·자동필터)으로 저장한다. 기존 `processor.py`의 서식 로직을 이식하되, 색은 `_row_color`에서, 시트 분리는 열 이름으로 한다.

**Files:**
- Create: `app/core/excel_writer.py`
- Test: `tests/test_excel_writer.py`

**Interfaces:**
- Consumes: Task 6의 `process` 결과 df (열 `점검순위`, `_row_color` 포함)
- Produces:
  - `write_excel(df: pd.DataFrame, output_path: str, sheet_split_column: str = "") -> str`
    - `_row_color` 내부 열은 출력에서 제외
    - `sheet_split_column`이 비었거나 df에 없으면 메인 시트만 생성
    - 각 순위 행 배경을 `_row_color`(비어있지 않을 때)로 채움
    - 반환값: `output_path`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_excel_writer.py`:
```python
import pandas as pd
from openpyxl import load_workbook
from app.core.excel_writer import write_excel


def _processed_df():
    return pd.DataFrame({
        "지사": ["강릉", "강릉", "원주"],
        "공사명": ["활선", "일반", "변전"],
        "점검순위": ["1순위", "3순위", "2순위"],
        "_row_color": ["#FFFF00", "", "#F7B9AF"],
    })


def test_writes_file_and_splits_sheets(tmp_path):
    out = tmp_path / "결과.xlsx"
    write_excel(_processed_df(), str(out), sheet_split_column="지사")
    assert out.exists()
    wb = load_workbook(out)
    # 메인 시트 + 지사 고유값 2개(강릉, 원주)
    assert "강릉" in wb.sheetnames
    assert "원주" in wb.sheetnames


def test_internal_color_column_excluded(tmp_path):
    out = tmp_path / "결과.xlsx"
    write_excel(_processed_df(), str(out), sheet_split_column="지사")
    wb = load_workbook(out)
    ws = wb[wb.sheetnames[0]]
    headers = [c.value for c in ws[1]]
    assert "_row_color" not in headers
    assert "점검순위" in headers


def test_no_split_column_single_sheet(tmp_path):
    out = tmp_path / "결과.xlsx"
    write_excel(_processed_df(), str(out), sheet_split_column="")
    wb = load_workbook(out)
    assert len(wb.sheetnames) == 1


def test_priority_row_fill_applied(tmp_path):
    out = tmp_path / "결과.xlsx"
    write_excel(_processed_df(), str(out), sheet_split_column="")
    wb = load_workbook(out)
    ws = wb[wb.sheetnames[0]]
    # 첫 데이터 행(2행)은 1순위 → 노란색 채움
    fill = ws.cell(row=2, column=1).fill
    assert fill.fgColor.rgb.endswith("FFFF00")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_excel_writer.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: 구현 작성**

`app/core/excel_writer.py`:
```python
"""엑셀 생성기 — 가공된 DataFrame → 지사별 다중 시트 + 서식 xlsx"""

import pandas as pd
from openpyxl.styles import Alignment, Font, PatternFill, Border, Side

INTERNAL_COLOR_COL = "_row_color"


def _safe_sheet_name(value: str) -> str:
    name = str(value)[:31]
    for ch in '/\\*[]:?':
        name = name.replace(ch, "_")
    return name or "시트"


def _hex(color: str) -> str:
    """'#FFFF00' → 'FFFF00' (openpyxl용). 빈 값이면 ''."""
    return color.lstrip("#").upper() if color else ""


def _apply_format(ws, colors: list[str], n_cols: int):
    """한 시트에 서식 적용. colors[i]는 데이터 i번째 행(엑셀 i+2행)의 배경색 헥사."""
    center = Alignment(horizontal="center", vertical="center", wrap_text=True)
    for row in ws.iter_rows(min_row=1, max_row=ws.max_row, min_col=1, max_col=n_cols):
        for cell in row:
            cell.alignment = center

    bold = Font(bold=True)
    header_fill = PatternFill(start_color="D3D3D3", end_color="D3D3D3", fill_type="solid")
    for col in range(1, n_cols + 1):
        c = ws.cell(row=1, column=col)
        c.font = bold
        c.fill = header_fill

    for i, hexcolor in enumerate(colors):
        if not hexcolor:
            continue
        fill = PatternFill(start_color=hexcolor, end_color=hexcolor, fill_type="solid")
        for col in range(1, n_cols + 1):
            ws.cell(row=i + 2, column=col).fill = fill

    thin = Side(style="thin", color="000000")
    border = Border(left=thin, right=thin, top=thin, bottom=thin)
    for row in ws.iter_rows(min_row=1, max_row=ws.max_row, min_col=1, max_col=n_cols):
        for cell in row:
            cell.border = border

    ws.freeze_panes = "A2"
    ws.auto_filter.ref = ws.dimensions


def write_excel(df: pd.DataFrame, output_path: str, sheet_split_column: str = "") -> str:
    colors_all = (
        df[INTERNAL_COLOR_COL].fillna("").tolist()
        if INTERNAL_COLOR_COL in df.columns else [""] * len(df)
    )
    visible = df.drop(columns=[INTERNAL_COLOR_COL], errors="ignore")
    n_cols = len(visible.columns)

    split = sheet_split_column if sheet_split_column in visible.columns else ""

    with pd.ExcelWriter(output_path, engine="openpyxl") as writer:
        # 메인 시트 (전체)
        visible.to_excel(writer, index=False, sheet_name="전체")

        sheet_colors = {"전체": [_hex(c) for c in colors_all]}

        if split:
            for value in sorted(visible[split].dropna().unique()):
                mask = (visible[split] == value).tolist()
                part = visible[visible[split] == value]
                part_colors = [_hex(c) for c, keep in zip(colors_all, mask) if keep]
                name = _safe_sheet_name(value)
                part.to_excel(writer, index=False, sheet_name=name)
                sheet_colors[name] = part_colors

        wb = writer.book
        for name, colors in sheet_colors.items():
            _apply_format(wb[name], colors, n_cols)

    return output_path
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_excel_writer.py -v`
Expected: PASS (4개)

- [ ] **Step 5: Commit**

```bash
git add app/core/excel_writer.py tests/test_excel_writer.py
git commit -m "feat: 엑셀 생성기(지사 시트 분리 + 순위 색·서식) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 다운로더 스키마 검증 분리 + 테스트

`downloader.py`의 "유효한 스키마인가" 판정을 순수 함수로 떼어내 테스트 가능하게 하고, 다운로드 경로에서 이를 사용한다. (열 스키마 이상 = 다운로드 실패 → 재시도, 기존 재시도 루프가 처리)

**Files:**
- Modify: `app/adapters/downloader.py`
- Test: `tests/test_downloader_schema.py`

**Interfaces:**
- Produces: `is_valid_schema(df: pd.DataFrame, min_columns: int = MIN_VALID_COLUMNS) -> bool`
  - 열 개수가 `min_columns` 미만이면 False
  - 그 외 True
- `download_excel_to_dataframe`는 `len(df.columns) < MIN_VALID_COLUMNS` 인라인 검사를 `is_valid_schema(df)` 호출로 대체(동작 동일, 반환 None 유지).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_downloader_schema.py`:
```python
import pandas as pd
from app.adapters.downloader import is_valid_schema


def test_single_column_is_invalid():
    df = pd.DataFrame({"오류": ["서버 오류 메시지"]})
    assert is_valid_schema(df) is False


def test_multi_column_is_valid():
    df = pd.DataFrame({"지사": ["강릉"], "공사명": ["활선"]})
    assert is_valid_schema(df) is True


def test_custom_min_columns():
    df = pd.DataFrame({"a": [1], "b": [2]})
    assert is_valid_schema(df, min_columns=3) is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_downloader_schema.py -v`
Expected: FAIL (`ImportError: cannot import name 'is_valid_schema'`)

- [ ] **Step 3: 구현 수정**

`app/adapters/downloader.py`의 `MIN_VALID_COLUMNS = 2` 아래에 함수 추가:
```python
def is_valid_schema(df, min_columns: int = MIN_VALID_COLUMNS) -> bool:
    """다운로드된 DataFrame이 유효한 스키마인지 판정.
    열 개수가 부족하면(서버 오류 응답 등) False → 호출부가 재시도."""
    return len(df.columns) >= min_columns
```

그리고 기존 인라인 검증부를 교체:
```python
            # Validate: server error returns 1-column DataFrame with error message
            if not is_valid_schema(df):
                first_value = str(df.iloc[0, 0]) if len(df) > 0 else ''
                print(f"[실패] 서버 오류 응답 (열 {len(df.columns)}개)")
                print(f"        내용: {first_value[:100]}")
                return None
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_downloader_schema.py -v`
Expected: PASS (3개)

- [ ] **Step 5: Commit**

```bash
git add app/adapters/downloader.py tests/test_downloader_schema.py
git commit -m "refactor: 다운로더 스키마 검증을 순수 함수(is_valid_schema)로 분리 + 테스트

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 골든 스냅샷 회귀 테스트 + 구 파일 정리

원본 df + 프리셋 → 최종 엑셀 파이프라인을 한 번에 검증하는 통합 테스트를 추가하고, 코어로 대체된 구 파일을 제거한다.

**Files:**
- Test: `tests/test_pipeline_golden.py`
- Delete: `processor.py`, `main.py`, `main_경남.py`

**Interfaces:**
- Consumes: `app.core.engine.process`, `app.core.excel_writer.write_excel`

- [ ] **Step 1: 통합(골든) 테스트 작성**

`tests/test_pipeline_golden.py`:
```python
import pandas as pd
from openpyxl import load_workbook
from app.core.engine import process
from app.core.excel_writer import write_excel
from app.core.settings import Preset, Rule, Filter


def test_full_pipeline_end_to_end(tmp_path):
    df = pd.DataFrame({
        "번호": [1, 2, 3, 4],
        "지사": ["강릉", "원주", "강릉", None],
        "공사명": ["활선 작업", "변전 점검", "일반 보수", "취소건"],
        "담당자": ["김", "이", "박", "최"],
    })
    preset = Preset(
        name="강원본부 기본", department_code="4200",
        filters=[Filter(column="지사", op="not_null")],   # None 행 제거 → 3행
        rules=[
            Rule(column="공사명", keyword="활선", match="contains", priority=1, color="#FFFF00"),
            Rule(column="공사명", keyword="변전", match="contains", priority=2, color="#F7B9AF"),
        ],
        drop_columns=["담당자"],
        sheet_split_column="지사",
        sort="priority",
    )

    processed = process(df, preset)
    # 필터: 4→3행, drop: 담당자 제거, 우선순위 라벨 존재
    assert len(processed) == 3
    assert "담당자" not in processed.columns
    assert processed.iloc[0]["점검순위"] == "1순위"

    out = tmp_path / "결과.xlsx"
    write_excel(processed, str(out), sheet_split_column="지사")
    wb = load_workbook(out)
    assert "전체" in wb.sheetnames
    assert "강릉" in wb.sheetnames and "원주" in wb.sheetnames
    ws = wb["전체"]
    headers = [c.value for c in ws[1]]
    assert "_row_color" not in headers and "담당자" not in headers
```

- [ ] **Step 2: 통합 테스트 통과 확인**

Run: `pytest tests/test_pipeline_golden.py -v`
Expected: PASS

- [ ] **Step 3: 구 파일 삭제**

```bash
git rm processor.py main.py main_경남.py
```

- [ ] **Step 4: 전체 테스트 스위트 통과 확인**

Run: `pytest -v`
Expected: PASS (모든 테스트; 실패·에러 0)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "test: 전체 파이프라인 골든 테스트 추가 + 구 processor/main 파일 삭제

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: requirements.txt 정리

pytest를 개발 의존성으로 추가하고, 더 이상 쓰지 않는 항목 주석을 정리한다. (PySide6는 Plan 2에서 추가)

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: requirements.txt 수정**

`requirements.txt` 끝에 추가:
```
# 개발/테스트
pytest>=8.0.0
```

- [ ] **Step 2: 설치·수집 확인**

Run: `pip install -r requirements.txt && pytest --collect-only -q`
Expected: 에러 없이 테스트 목록 출력

- [ ] **Step 3: Commit**

```bash
git add requirements.txt
git commit -m "chore: pytest 개발 의존성 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review (작성자 점검 결과)

**1. 스펙 커버리지 (Plan 1 범위):**
- 3계층 구조·어댑터 이동 → Task 1 ✓
- 본부 29개 매핑 → Task 2 ✓
- 프리셋 데이터 모델(Rule/Filter/Preset/날짜offset/메일필드) → Task 3 ✓
- 프리셋 JSON 저장소(presets/) → Task 4 ✓
- 범용 규칙 엔진(필터·drop·우선순위·정렬, 헤더명 매칭) → Task 5·6 ✓
- 엑셀 생성(지사 시트 분리·순위 색·서식) → Task 7 ✓
- 열 스키마 이상 → 재시도(실패 반환) → Task 8 ✓
- 단위 테스트·골든 스냅샷 → Task 2~9 ✓
- 구 파일·분류표 로직 폐기 → Task 1(config), Task 9(processor/main) ✓
- **Plan 2로 이월(범위 밖):** UI(`app/ui/*`), QThread 워커, 재시도 루프의 UI 이전, `build_exe.spec` 진입점 변경, PySide6 의존성, `{DATE}` 치환의 UI 연결(치환 자체는 기존 mailer가 보유).

**2. 플레이스홀더 스캔:** "TBD/TODO/적절히 처리" 없음. 모든 코드 스텝에 실제 코드 포함. ✓

**3. 타입 일관성:** `Preset`/`Rule`/`Filter` 필드명이 Task 3 정의와 5·6·7·9 사용에서 일치. `process`→df(`점검순위`,`_row_color`)→`write_excel` 소비 일치. `is_valid_schema` 시그니처 일치. ✓

**미결(설계 후속, Plan 2에서 확정):** 메인 시트명은 기존 C2값 대신 고정 `"전체"`로 단순화함(스펙의 "메인 시트"는 유지, 명칭만 안정화). 열 너비는 데이터 컬럼이 가변이라 Plan 1에선 생략하고 Plan 2 UI/서식 조정 단계에서 필요 시 재도입.
