# 단일 EXE 재설계 — Plan 2: PySide6 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plan 1의 순수 코어 위에 PySide6 대화형 데스크톱 UI를 올려, 날짜·본부 선택 → 추출 → 설정(drop·규칙·필터·색·정렬) → 엑셀 생성 → 메일 발송을 하나의 프로그램에서 수행하고, 설정을 프리셋으로 저장·재사용하는 단일 EXE를 완성한다.

**Architecture:** 좌측 사이드바(①추출/②설정/③메일) + 프리셋 바. 네트워크 작업(인증·다운로드·메일)은 `QThread` 워커에서 실행해 UI를 막지 않는다. 재시도·날짜·메일변환 등 판단 로직은 UI에서 분리한 순수 함수(`app/core/*`)로 두어 오프라인 단위 테스트한다. 코어(Plan 1)는 그대로 소비만 한다.

**Tech Stack:** Python 3.10+, PySide6, pandas, openpyxl, pytest. 코어: `app/core/*` (Plan 1).

관련 스펙: `docs/superpowers/specs/2026-07-02-단일exe-대화형-재설계-design.md`
선행 계획: `docs/superpowers/plans/2026-07-02-단일exe-재설계-1-코어.md` (완료·병합됨)

## 확정된 코어 인터페이스 (Plan 2가 호출; 변경 금지)

```python
# app/core/engine.py
def process(df: pd.DataFrame, preset: Preset) -> pd.DataFrame   # 중복 헤더 시 ValueError
# app/core/excel_writer.py
def write_excel(df, output_path: str, sheet_split_column: str = "") -> str
# app/core/settings.py
Rule(column,keyword,match="contains",priority=3,color="#FFFFFF")
Filter(column,op,value="")            # op: not_null|equals|contains|not_equals
Preset(name, department_code, default_date_offset=1, drop_columns=[], sheet_split_column="",
       rules=[], filters=[], sort="none", mail_from_name="", mail_from_email="",
       mail_recipients=[], mail_subject="", mail_body="")
Preset.to_dict() -> dict ; Preset.from_dict(d) -> Preset
# app/core/preset_store.py  (directory: Path)
save_preset(preset, directory) -> Path ; load_preset(name, directory) -> Preset
list_presets(directory) -> list[str] ; delete_preset(name, directory) -> None
# app/core/departments.py
all_departments() -> list[Department]         # Department(.code,.name,.group)
grouped() -> dict[str, list[Department]]       # 키순서 지역본부/건설본부/본사
name_for_code(code)->str|None ; code_for_name(name)->str|None
# app/adapters/config.py
get_base_dir() -> str ; get_output_dir() -> str
# app/adapters/auth.py
authenticate() -> requests.Session | None
# app/adapters/downloader.py
download_excel_to_dataframe(session, date_from=None, date_to=None, department_code=None) -> DataFrame|None
# app/adapters/mailer.py  (mail_config는 dict!)
send_bizmail(session, mail_config, attachment_paths=None, date_yymmdd=None, date_yy_mm_dd=None) -> dict
#   mail_config keys: from_name, from_email, recipients(list), subject, body
#   반환 dict: {'success': bool, 'message': str, ...}
```

## Global Constraints

- 모든 UI 텍스트·주석·문서는 한국어.
- 코어(`app/core/*`)는 수정하지 않는다(호출만). 새 순수 로직은 `app/core/`에 새 파일로 추가하고 Qt를 import하지 않는다.
- 네트워크 작업(auth/download/mail)은 반드시 `QThread` 워커에서 실행 — UI 스레드에서 직접 호출 금지.
- 우선순위 라벨은 정확히 `"1순위"/"2순위"/"3순위"`. 색은 헥사(`#RRGGBB`).
- `engine.process`는 중복 헤더 시 `ValueError`를 던지므로 호출부는 반드시 try/except로 감싸 사용자에게 표시.
- 미리보기는 **샘플만**(상위 20행). 전체 데이터는 [엑셀 생성] 시점에만 처리.
- 절대 임포트(`from app.core.settings import ...`). 테스트는 저장소 루트에서 `pytest`.
- PySide6 위젯 로직 테스트는 자동화하지 않는다(수동 검증). 순수 함수만 pytest 대상.
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

## 파일 구조 (이 계획이 생성)

```
app/
  main.py                      진입점 (QApplication)  [Task 10]
  app_paths.py                 presets/output 경로   [Task 1]
  core/
    date_util.py               날짜 계산/포맷          [Task 2]
    mail_config.py             Preset→mail_config dict [Task 2]
    pipeline.py                인증+다운로드 재시도(순수) [Task 3]
  ui/
    theme.qss                  스타일               [Task 4]
    state.py                   AppState(공유 상태)     [Task 4]
    workers.py                 DownloadWorker/MailWorker [Task 5]
    main_window.py             사이드바+스택+프리셋바    [Task 6]
    extract_view.py            ① 날짜·본부·[불러오기]   [Task 7]
    configure_view.py          ② 설정+샘플미리보기       [Task 8]
    mail_view.py               ③ 메일·[발송]           [Task 9]
tests/
  test_date_util.py test_mail_config.py test_pipeline.py test_app_paths.py
build_exe.spec                 진입점 app/main.py      [Task 11]
requirements.txt               + PySide6              [Task 11]
```

---

### Task 1: 앱 경로 헬퍼 (app_paths.py)

프리셋 저장 폴더와 출력 폴더 경로를 한 곳에서 제공한다. `app/`(코어 아님)에 두어 코어 순수성을 유지한다.

**Files:**
- Create: `app/app_paths.py`
- Test: `tests/test_app_paths.py`

**Interfaces:**
- Consumes: `app.adapters.config.get_base_dir`
- Produces:
  - `presets_dir() -> Path` — `<base>/presets`, 없으면 생성
  - `output_dir() -> Path` — `<base>` (엑셀 저장 위치)

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_app_paths.py`:
```python
from pathlib import Path
import app.app_paths as ap


def test_presets_dir_created_under_base(monkeypatch, tmp_path):
    monkeypatch.setattr(ap.config, "get_base_dir", lambda: str(tmp_path))
    d = ap.presets_dir()
    assert d == tmp_path / "presets"
    assert d.exists()


def test_output_dir_is_base(monkeypatch, tmp_path):
    monkeypatch.setattr(ap.config, "get_base_dir", lambda: str(tmp_path))
    assert ap.output_dir() == tmp_path
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_app_paths.py -v`
Expected: FAIL (`ModuleNotFoundError: app.app_paths`)

- [ ] **Step 3: 구현**

`app/app_paths.py`:
```python
"""앱 런타임 경로 — 프리셋 폴더와 엑셀 출력 폴더"""

from pathlib import Path
from app.adapters import config


def presets_dir() -> Path:
    d = Path(config.get_base_dir()) / "presets"
    d.mkdir(parents=True, exist_ok=True)
    return d


def output_dir() -> Path:
    return Path(config.get_base_dir())
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_app_paths.py -v`
Expected: PASS (2)

- [ ] **Step 5: Commit**

```bash
git add app/app_paths.py tests/test_app_paths.py
git commit -m "feat(ui): 앱 경로 헬퍼(presets/output) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 날짜·메일변환 순수 헬퍼 (date_util.py, mail_config.py)

날짜 기본값 계산/포맷과 Preset→mail_config dict 변환을 순수 함수로 만든다.

**Files:**
- Create: `app/core/date_util.py`, `app/core/mail_config.py`
- Test: `tests/test_date_util.py`, `tests/test_mail_config.py`

**Interfaces:**
- Produces:
  - `resolve_default_date(today: date, offset: int) -> date`
  - `date_formats(d: date) -> dict` → 키 `ymd`("%Y-%m-%d"), `yymmdd`("%y%m%d"), `yy_mm_dd`("'%y-%m-%d")
  - `preset_to_mail_config(preset: Preset) -> dict` → 키 from_name/from_email/recipients/subject/body

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_date_util.py`:
```python
from datetime import date
from app.core.date_util import resolve_default_date, date_formats


def test_resolve_default_date_offsets():
    base = date(2026, 3, 1)
    assert resolve_default_date(base, 0) == date(2026, 3, 1)
    assert resolve_default_date(base, 1) == date(2026, 3, 2)
    assert resolve_default_date(base, -1) == date(2026, 2, 28)


def test_date_formats():
    f = date_formats(date(2025, 3, 1))
    assert f["ymd"] == "2025-03-01"
    assert f["yymmdd"] == "250301"
    assert f["yy_mm_dd"] == "'25-03-01"
```

`tests/test_mail_config.py`:
```python
from app.core.mail_config import preset_to_mail_config
from app.core.settings import Preset


def test_preset_to_mail_config_maps_fields():
    p = Preset(name="t", department_code="4200",
               mail_from_name="홍길동", mail_from_email="hong@kepco.co.kr",
               mail_recipients=["a@kepco.co.kr", "b@kepco.co.kr"],
               mail_subject="{DATE} 리스트", mail_body="본문 {DATE}")
    mc = preset_to_mail_config(p)
    assert mc == {
        "from_name": "홍길동",
        "from_email": "hong@kepco.co.kr",
        "recipients": ["a@kepco.co.kr", "b@kepco.co.kr"],
        "subject": "{DATE} 리스트",
        "body": "본문 {DATE}",
    }
    # 원본 리스트와 분리(복사)되어야 함
    mc["recipients"].append("x")
    assert p.mail_recipients == ["a@kepco.co.kr", "b@kepco.co.kr"]
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_date_util.py tests/test_mail_config.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 구현**

`app/core/date_util.py`:
```python
"""날짜 기본값 계산과 파이프라인용 포맷 (순수)"""

from datetime import date, timedelta


def resolve_default_date(today: date, offset: int) -> date:
    """today + offset일. offset: 0=오늘, 1=내일, -1=어제 …"""
    return today + timedelta(days=offset)


def date_formats(d: date) -> dict:
    """파이프라인 각 단계가 쓰는 3가지 날짜 문자열."""
    return {
        "ymd": d.strftime("%Y-%m-%d"),        # downloader date_from
        "yymmdd": d.strftime("%y%m%d"),       # 파일명, 메일 제목 {DATE}
        "yy_mm_dd": d.strftime("'%y-%m-%d"),  # 메일 본문 {DATE}
    }
```

`app/core/mail_config.py`:
```python
"""Preset의 메일 필드를 mailer.send_bizmail이 받는 dict로 변환 (순수)"""

from app.core.settings import Preset


def preset_to_mail_config(preset: Preset) -> dict:
    return {
        "from_name": preset.mail_from_name,
        "from_email": preset.mail_from_email,
        "recipients": list(preset.mail_recipients),
        "subject": preset.mail_subject,
        "body": preset.mail_body,
    }
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_date_util.py tests/test_mail_config.py -v`
Expected: PASS (4)

- [ ] **Step 5: Commit**

```bash
git add app/core/date_util.py app/core/mail_config.py tests/test_date_util.py tests/test_mail_config.py
git commit -m "feat: 날짜 헬퍼 + Preset→mail_config 변환 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 인증+다운로드 재시도 오케스트레이션 (pipeline.py)

기존 `main.py`의 "인증+다운로드 최대 10회 재시도" 루프를, 주입된 콜러블로 동작하는 순수 함수로 옮긴다. 워커(Task 5)가 이걸 실제 어댑터와 함께 호출한다.

**Files:**
- Create: `app/core/pipeline.py`
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Produces:
  - `run_auth_and_download(authenticate, download, *, date_from, department_code, max_retries=10, delay=3, sleep=time.sleep, on_status=None) -> tuple`
    - `authenticate() -> session|None`, `download(session, date_from, department_code) -> df|None`
    - 성공 시 `(session, df)` 반환; `max_retries` 소진 시 `(None, None)`
    - 매 시도 전 `on_status(문자열)` 호출(있으면). 재시도 사이 `sleep(delay)` (첫 시도 제외)

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_pipeline.py`:
```python
import pandas as pd
from app.core.pipeline import run_auth_and_download


def test_succeeds_first_try():
    df = pd.DataFrame({"a": [1], "b": [2]})
    calls = []
    session, out = run_auth_and_download(
        authenticate=lambda: "SESSION",
        download=lambda s, date_from, department_code: df,
        date_from="2025-03-01", department_code="4200",
        sleep=lambda _: calls.append("slept"),
    )
    assert session == "SESSION" and out is df
    assert calls == []  # 재시도 없음 → sleep 없음


def test_retries_then_succeeds():
    df = pd.DataFrame({"a": [1], "b": [2]})
    attempts = {"n": 0}

    def auth():
        return "S"

    def dl(s, date_from, department_code):
        attempts["n"] += 1
        return None if attempts["n"] < 3 else df

    statuses = []
    session, out = run_auth_and_download(
        authenticate=auth, download=dl,
        date_from="2025-03-01", department_code="4200",
        max_retries=5, sleep=lambda _: None,
        on_status=statuses.append,
    )
    assert out is df and attempts["n"] == 3
    assert len(statuses) == 3  # 시도마다 상태 1회


def test_gives_up_after_max_retries():
    session, out = run_auth_and_download(
        authenticate=lambda: None,       # 인증 계속 실패
        download=lambda *a, **k: None,
        date_from="2025-03-01", department_code="4200",
        max_retries=4, sleep=lambda _: None,
    )
    assert session is None and out is None
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_pipeline.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 구현**

`app/core/pipeline.py`:
```python
"""인증+다운로드 재시도 오케스트레이션 (순수 — 콜러블 주입).

UI 워커가 실제 auth/downloader 함수를 주입해 호출한다. 열 스키마 이상은
downloader가 None을 반환하므로 여기서 재시도로 처리된다."""

import time


def run_auth_and_download(authenticate, download, *, date_from, department_code,
                          max_retries=10, delay=3, sleep=time.sleep, on_status=None):
    for attempt in range(1, max_retries + 1):
        if attempt > 1:
            sleep(delay)
        if on_status:
            on_status(f"인증+다운로드 시도 {attempt}/{max_retries}…")
        session = authenticate()
        if session is None:
            continue
        df = download(session, date_from, department_code)
        if df is not None:
            return session, df
    return None, None
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_pipeline.py -v`
Expected: PASS (3)

- [ ] **Step 5: Commit**

```bash
git add app/core/pipeline.py tests/test_pipeline.py
git commit -m "feat: 인증+다운로드 재시도 오케스트레이션(순수) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 공유 상태 + 테마 (state.py, theme.qss)

뷰 간 공유되는 런타임 상태(원본 df, 세션, 편집 중 Preset, 생성된 파일 경로)와 QSS 테마를 만든다.

**Files:**
- Create: `app/ui/__init__.py`, `app/ui/state.py`, `app/ui/theme.qss`

**Interfaces:**
- Produces:
  - `AppState` (일반 객체): 속성 `df` (pd.DataFrame|None), `session` (auth 세션|None), `preset` (Preset), `output_path` (str|None). 메서드 `columns() -> list[str]` (df 컬럼명, 없으면 []).

- [ ] **Step 1: 패키지·상태 파일 생성**

`app/ui/__init__.py`: (빈 파일)

`app/ui/state.py`:
```python
"""뷰 간 공유되는 런타임 상태."""

from app.core.settings import Preset


class AppState:
    def __init__(self):
        self.df = None            # 다운로드 원본 DataFrame
        self.session = None       # 인증 세션 (메일 발송에 재사용)
        self.preset = Preset(name="새 프리셋", department_code="")
        self.output_path = None   # 생성된 엑셀 경로

    def columns(self) -> list:
        if self.df is None:
            return []
        return list(self.df.columns)
```

- [ ] **Step 2: 테마 작성**

`app/ui/theme.qss`:
```css
* { font-family: "Malgun Gothic", "맑은 고딕", sans-serif; font-size: 13px; color: #1f2733; }
QMainWindow, QWidget { background: #f4f6f9; }
#Sidebar { background: #1f3a5f; min-width: 176px; max-width: 176px; }
#Sidebar QListWidget { background: transparent; border: none; color: #cdd7e5; outline: 0; }
#Sidebar QListWidget::item { padding: 13px 18px; border: none; }
#Sidebar QListWidget::item:selected { background: #2b4d7a; color: #ffffff; border-left: 3px solid #6ea8fe; }
#SidebarTitle { color: #ffffff; font-size: 15px; font-weight: 700; padding: 18px; }
#Card { background: #ffffff; border: 1px solid #e3e8ef; border-radius: 8px; }
QLabel#H1 { font-size: 18px; font-weight: 700; }
QLabel#Hint { color: #7a8699; }
QPushButton { background: #1f3a5f; color: #fff; border: none; border-radius: 6px; padding: 8px 16px; font-weight: 600; }
QPushButton:hover { background: #29507f; }
QPushButton:disabled { background: #c3ccd8; color: #eef1f5; }
QPushButton#Ghost { background: #e8edf4; color: #1f3a5f; }
QPushButton#Ghost:hover { background: #dbe3ee; }
QLineEdit, QComboBox, QDateEdit, QSpinBox, QPlainTextEdit, QListWidget, QTableWidget {
    background: #fff; border: 1px solid #cfd6e0; border-radius: 6px; padding: 5px; }
QTableWidget { gridline-color: #e8ecf2; }
QHeaderView::section { background: #eef1f6; border: none; padding: 6px; font-weight: 700; }
#StatusLog { background: #0f1c2e; color: #cfe0f5; border-radius: 6px; font-family: Consolas, monospace; font-size: 12px; }
```

- [ ] **Step 3: 임포트 스모크 확인**

Run: `python -c "import app.ui.state; from app.ui.state import AppState; s=AppState(); print(s.columns())"`
Expected: `[]`

- [ ] **Step 4: Commit**

```bash
git add app/ui/__init__.py app/ui/state.py app/ui/theme.qss
git commit -m "feat(ui): 공유 상태(AppState) + QSS 테마 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: QThread 워커 (workers.py)

네트워크 작업을 백그라운드에서 실행하는 두 워커. UI 스레드를 막지 않고 시그널로 상태·결과를 전달한다.

**Files:**
- Create: `app/ui/workers.py`

**Interfaces:**
- Consumes: `app.core.pipeline.run_auth_and_download`, `app.adapters.auth`, `app.adapters.downloader`, `app.adapters.mailer`
- Produces:
  - `DownloadWorker(QThread)` — 생성자 `(date_from: str, department_code: str)`. 시그널 `status(str)`, `done(object, object)` (session, df), `failed(str)`. `run()`에서 `run_auth_and_download` 호출.
  - `MailWorker(QThread)` — 생성자 `(session, mail_config: dict, attachment_path: str, date_yymmdd: str, date_yy_mm_dd: str)`. 시그널 `status(str)`, `done(dict)`, `failed(str)`.

- [ ] **Step 1: 구현**

`app/ui/workers.py`:
```python
"""네트워크 작업용 QThread 워커 (UI 스레드 비차단)."""

from PySide6.QtCore import QThread, Signal

from app.core.pipeline import run_auth_and_download
from app.adapters import auth, downloader, mailer


class DownloadWorker(QThread):
    status = Signal(str)
    done = Signal(object, object)   # (session, df)
    failed = Signal(str)

    def __init__(self, date_from: str, department_code: str):
        super().__init__()
        self._date_from = date_from
        self._dept = department_code

    def run(self):
        try:
            session, df = run_auth_and_download(
                authenticate=auth.authenticate,
                download=downloader.download_excel_to_dataframe,
                date_from=self._date_from,
                department_code=self._dept,
                on_status=self.status.emit,
            )
            if df is None:
                self.failed.emit(
                    "인증+다운로드에 실패했습니다.\n"
                    "· PowerGate 실행 여부\n· 사내망 연결\n· 해당 날짜 데이터 유무를 확인하세요."
                )
            else:
                self.done.emit(session, df)
        except Exception as e:  # noqa: BLE001
            self.failed.emit(f"예상치 못한 오류: {e}")


class MailWorker(QThread):
    status = Signal(str)
    done = Signal(dict)
    failed = Signal(str)

    def __init__(self, session, mail_config: dict, attachment_path: str,
                 date_yymmdd: str, date_yy_mm_dd: str):
        super().__init__()
        self._session = session
        self._mail_config = mail_config
        self._attachment = attachment_path
        self._yymmdd = date_yymmdd
        self._yy_mm_dd = date_yy_mm_dd

    def run(self):
        try:
            self.status.emit("메일 전송 중…")
            result = mailer.send_bizmail(
                session=self._session,
                mail_config=self._mail_config,
                attachment_paths=[self._attachment] if self._attachment else [],
                date_yymmdd=self._yymmdd,
                date_yy_mm_dd=self._yy_mm_dd,
            )
            self.done.emit(result)
        except Exception as e:  # noqa: BLE001
            self.failed.emit(f"메일 전송 오류: {e}")
```

- [ ] **Step 2: 임포트 스모크 확인**

Run: `python -c "import app.ui.workers; print('ok')"`
Expected: `ok` (PySide6 미설치면 먼저 `pip install PySide6`; Task 11에서 requirements에 추가)

- [ ] **Step 3: Commit**

```bash
git add app/ui/workers.py
git commit -m "feat(ui): 다운로드/메일 QThread 워커 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

**수동 검증(사내망):** 이 태스크는 자동 테스트 대상 아님. Task 10 통합 실행 시 함께 검증.

---

### Task 6: 메인 윈도우 + 사이드바 + 프리셋 바 (main_window.py)

좌측 사이드바(①추출/②설정/③메일)와 `QStackedWidget` 본문, 하단 프리셋 저장/불러오기 바를 구성한다. 뷰 전환과 프리셋 로드/세이브를 담당한다.

**Files:**
- Create: `app/ui/main_window.py`

**Interfaces:**
- Consumes: `AppState`, `ExtractView`(Task7), `ConfigureView`(Task8), `MailView`(Task9), `app.core.preset_store`, `app.app_paths`, `departments`
- Produces:
  - `MainWindow(QMainWindow)` — 메서드 `goto(index:int)` (스택 전환+사이드바 동기화), `apply_preset(preset)` (각 뷰에 반영), `collect_preset()->Preset` (각 뷰에서 현재 설정 수집).

- [ ] **Step 1: 구현**

`app/ui/main_window.py`:
```python
"""메인 윈도우 — 사이드바 네비게이션 + 프리셋 바."""

from PySide6.QtWidgets import (
    QMainWindow, QWidget, QHBoxLayout, QVBoxLayout, QListWidget,
    QStackedWidget, QLabel, QPushButton, QComboBox, QInputDialog, QMessageBox,
)

from app.ui.state import AppState
from app.ui.extract_view import ExtractView
from app.ui.configure_view import ConfigureView
from app.ui.mail_view import MailView
from app.core import preset_store
from app import app_paths


class MainWindow(QMainWindow):
    def __init__(self):
        super().__init__()
        self.setWindowTitle("KEPCO 점검 리스트 생성기")
        self.resize(1080, 720)
        self.state = AppState()

        # 사이드바
        sidebar = QWidget(objectName="Sidebar")
        sb = QVBoxLayout(sidebar)
        sb.setContentsMargins(0, 0, 0, 0)
        sb.addWidget(QLabel("KEPCO RPA", objectName="SidebarTitle"))
        self.nav = QListWidget()
        self.nav.addItems(["①  추출", "②  설정", "③  메일"])
        self.nav.currentRowChanged.connect(self._nav_changed)
        sb.addWidget(self.nav)
        sb.addStretch(1)
        sb.addWidget(self._preset_bar())

        # 본문 스택
        self.stack = QStackedWidget()
        self.extract = ExtractView(self.state, self)
        self.configure = ConfigureView(self.state, self)
        self.mail = MailView(self.state, self)
        for w in (self.extract, self.configure, self.mail):
            self.stack.addWidget(w)

        root = QWidget()
        row = QHBoxLayout(root)
        row.setContentsMargins(0, 0, 0, 0)
        row.setSpacing(0)
        row.addWidget(sidebar)
        row.addWidget(self.stack, 1)
        self.setCentralWidget(root)
        self.nav.setCurrentRow(0)

    def _preset_bar(self) -> QWidget:
        bar = QWidget()
        v = QVBoxLayout(bar)
        v.setContentsMargins(12, 12, 12, 16)
        self.preset_combo = QComboBox()
        self._reload_presets()
        load = QPushButton("불러오기", objectName="Ghost")
        save = QPushButton("현재 설정 저장", objectName="Ghost")
        load.clicked.connect(self._load_preset)
        save.clicked.connect(self._save_preset)
        for w in (QLabel("프리셋", objectName="Hint"), self.preset_combo, load, save):
            v.addWidget(w)
        return bar

    # ---- 네비게이션 ----
    def _nav_changed(self, row: int):
        if row >= 0:
            self.stack.setCurrentIndex(row)

    def goto(self, index: int):
        self.nav.setCurrentRow(index)

    # ---- 프리셋 ----
    def _reload_presets(self):
        self.preset_combo.clear()
        self.preset_combo.addItems(preset_store.list_presets(app_paths.presets_dir()))

    def _load_preset(self):
        name = self.preset_combo.currentText()
        if not name:
            return
        preset = preset_store.load_preset(name, app_paths.presets_dir())
        self.state.preset = preset
        self.apply_preset(preset)
        QMessageBox.information(self, "프리셋", f"'{name}' 불러오기 완료")

    def _save_preset(self):
        preset = self.collect_preset()
        name, ok = QInputDialog.getText(self, "프리셋 저장", "이름:", text=preset.name)
        if not ok or not name.strip():
            return
        preset.name = name.strip()
        preset_store.save_preset(preset, app_paths.presets_dir())
        self.state.preset = preset
        self._reload_presets()
        self.preset_combo.setCurrentText(preset.name)
        QMessageBox.information(self, "프리셋", f"'{preset.name}' 저장 완료")

    def apply_preset(self, preset):
        self.extract.apply_preset(preset)
        self.configure.apply_preset(preset)
        self.mail.apply_preset(preset)

    def collect_preset(self):
        preset = self.state.preset
        self.extract.write_into(preset)
        self.configure.write_into(preset)
        self.mail.write_into(preset)
        return preset
```

- [ ] **Step 2: Commit** (뷰 3개가 있어야 import 가능 → Task 7~9 이후 통합 스모크. 지금은 파일만 커밋)

```bash
git add app/ui/main_window.py
git commit -m "feat(ui): 메인 윈도우(사이드바+스택+프리셋 바) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

**참고:** 이 파일은 Task 7~9의 뷰(`apply_preset`/`write_into` 인터페이스)를 소비한다. 통합 실행 스모크는 Task 10에서 수행.

---

### Task 7: 추출 뷰 (extract_view.py)

날짜 선택기(프리셋 기본 offset로 초기화) + 본부 드롭다운(그룹) + [불러오기] 버튼 + 상태 로그. 다운로드 워커를 구동하고 성공 시 설정 화면으로 넘긴다.

**Files:**
- Create: `app/ui/extract_view.py`

**Interfaces:**
- Consumes: `AppState`, `DownloadWorker`, `departments.grouped/all_departments/name_for_code`, `date_util`, `app_paths`
- Produces:
  - `ExtractView(QWidget)` — `apply_preset(preset)`, `write_into(preset)`, 그리고 다운로드 완료 시 `self.state.df/session` 세팅 후 `main.configure.load_dataframe()` 호출 + `main.goto(1)`.

- [ ] **Step 1: 구현**

`app/ui/extract_view.py`:
```python
"""① 추출 뷰 — 날짜·본부 선택 후 다운로드."""

from datetime import date

from PySide6.QtCore import QDate
from PySide6.QtWidgets import (
    QWidget, QVBoxLayout, QFormLayout, QComboBox, QDateEdit, QPushButton,
    QLabel, QPlainTextEdit,
)

from app.core import departments
from app.core.date_util import resolve_default_date
from app.ui.workers import DownloadWorker


class ExtractView(QWidget):
    def __init__(self, state, main):
        super().__init__()
        self.state = state
        self.main = main
        self._worker = None
        self._offset = 1

        v = QVBoxLayout(self)
        v.setContentsMargins(28, 24, 28, 24)
        v.addWidget(QLabel("① 데이터 추출", objectName="H1"))
        v.addWidget(QLabel("날짜와 본부를 선택하고 불러오세요.", objectName="Hint"))

        form = QFormLayout()
        self.date_edit = QDateEdit()
        self.date_edit.setCalendarPopup(True)
        self.date_edit.setDisplayFormat("yyyy-MM-dd")
        self._reset_date()

        self.dept_combo = QComboBox()
        for group, deps in departments.grouped().items():
            self.dept_combo.addItem(f"── {group} ──")
            idx = self.dept_combo.count() - 1
            self.dept_combo.model().item(idx).setEnabled(False)
            for d in deps:
                self.dept_combo.addItem(d.name, d.code)

        form.addRow("대상 날짜", self.date_edit)
        form.addRow("본부", self.dept_combo)
        v.addLayout(form)

        self.btn = QPushButton("불러오기")
        self.btn.clicked.connect(self._start)
        v.addWidget(self.btn)

        self.log = QPlainTextEdit(objectName="StatusLog", readOnly=True)
        v.addWidget(self.log, 1)

    def _reset_date(self):
        d = resolve_default_date(date.today(), self._offset)
        self.date_edit.setDate(QDate(d.year, d.month, d.day))

    # ---- 프리셋 연동 ----
    def apply_preset(self, preset):
        self._offset = preset.default_date_offset
        self._reset_date()
        i = self.dept_combo.findData(preset.department_code)
        if i >= 0:
            self.dept_combo.setCurrentIndex(i)

    def write_into(self, preset):
        preset.department_code = self.dept_combo.currentData() or ""
        preset.default_date_offset = self._offset

    # ---- 다운로드 ----
    def _start(self):
        code = self.dept_combo.currentData()
        if not code:
            self._append("본부를 선택하세요.")
            return
        qd = self.date_edit.date()
        date_from = f"{qd.year():04d}-{qd.month():02d}-{qd.day():02d}"
        self.state.preset.department_code = code
        self.btn.setEnabled(False)
        self.log.clear()
        self._append(f"대상: {date_from} / {self.dept_combo.currentText()}")

        self._worker = DownloadWorker(date_from, code)
        self._worker.status.connect(self._append)
        self._worker.done.connect(self._on_done)
        self._worker.failed.connect(self._on_failed)
        self._worker.start()

    def _on_done(self, session, df):
        self.state.session = session
        self.state.df = df
        self._append(f"✅ 완료: {len(df)}행 × {len(df.columns)}열")
        self.btn.setEnabled(True)
        self.main.configure.load_dataframe()
        self.main.goto(1)

    def _on_failed(self, msg):
        self._append("❌ " + msg)
        self.btn.setEnabled(True)

    def _append(self, text):
        self.log.appendPlainText(text)
```

- [ ] **Step 2: Commit**

```bash
git add app/ui/extract_view.py
git commit -m "feat(ui): 추출 뷰(날짜·본부·다운로드) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 설정 뷰 (configure_view.py)

상단: 컬럼 Drop(체크), 우선순위 규칙 빌더(열·키워드·순위·색), 필터 빌더(열·연산·값), 정렬·시트분리 열 선택. 하단: 샘플 미리보기(상위 20행). [엑셀 생성] 시 `engine.process`+`write_excel` 실행 후 메일 화면으로.

**Files:**
- Create: `app/ui/configure_view.py`

**Interfaces:**
- Consumes: `AppState`, `engine.process`, `excel_writer.write_excel`, `settings.Rule/Filter`, `date_util`, `app_paths`
- Produces:
  - `ConfigureView(QWidget)` — `load_dataframe()` (state.df로 컬럼 위젯·미리보기 채움), `apply_preset(preset)`, `write_into(preset)`, `build_preset_settings()` (위젯 → Rule/Filter/drop/sort/split를 state.preset에 기록).

- [ ] **Step 1: 구현**

`app/ui/configure_view.py`:
```python
"""② 설정 뷰 — drop·규칙·필터·색·정렬 + 샘플 미리보기."""

from datetime import date

from PySide6.QtWidgets import (
    QWidget, QVBoxLayout, QHBoxLayout, QLabel, QPushButton, QListWidget,
    QListWidgetItem, QTableWidget, QTableWidgetItem, QComboBox, QLineEdit,
    QSpinBox, QColorDialog, QMessageBox, QGroupBox, QFormLayout,
)
from PySide6.QtCore import Qt
from PySide6.QtGui import QColor

from app.core.engine import process
from app.core.excel_writer import write_excel
from app.core.settings import Rule, Filter
from app.core.date_util import date_formats
from app import app_paths

PREVIEW_ROWS = 20
FILTER_OPS = ["not_null", "equals", "contains", "not_equals"]


class ConfigureView(QWidget):
    def __init__(self, state, main):
        super().__init__()
        self.state = state
        self.main = main

        v = QVBoxLayout(self)
        v.setContentsMargins(28, 24, 28, 24)
        v.addWidget(QLabel("② 설정", objectName="H1"))
        v.addWidget(QLabel("컬럼 제외·우선순위 규칙·필터를 지정하세요.", objectName="Hint"))

        top = QHBoxLayout()
        top.addWidget(self._drop_box(), 1)
        top.addWidget(self._rules_box(), 2)
        top.addWidget(self._filters_box(), 2)
        v.addLayout(top)

        opt = QFormLayout()
        self.sort_combo = QComboBox()
        self.split_combo = QComboBox()
        opt.addRow("정렬", self.sort_combo)
        opt.addRow("시트 분리 열", self.split_combo)
        v.addLayout(opt)

        v.addWidget(QLabel(f"샘플 미리보기 (상위 {PREVIEW_ROWS}행)", objectName="Hint"))
        self.preview = QTableWidget()
        v.addWidget(self.preview, 1)

        self.gen_btn = QPushButton("엑셀 생성 ▸")
        self.gen_btn.clicked.connect(self._generate)
        v.addWidget(self.gen_btn)

    # ---- 섹션 위젯 ----
    def _drop_box(self):
        box = QGroupBox("컬럼 Drop (체크=제외)")
        lay = QVBoxLayout(box)
        self.drop_list = QListWidget()
        lay.addWidget(self.drop_list)
        return box

    def _rules_box(self):
        box = QGroupBox("우선순위 규칙")
        lay = QVBoxLayout(box)
        self.rules_table = QTableWidget(0, 5)
        self.rules_table.setHorizontalHeaderLabels(["열", "키워드", "매칭", "순위", "색"])
        lay.addWidget(self.rules_table)
        row = QHBoxLayout()
        add = QPushButton("＋ 규칙", objectName="Ghost")
        rm = QPushButton("－ 삭제", objectName="Ghost")
        add.clicked.connect(lambda: self._add_rule_row())
        rm.clicked.connect(lambda: self._remove_row(self.rules_table))
        row.addWidget(add); row.addWidget(rm)
        lay.addLayout(row)
        return box

    def _filters_box(self):
        box = QGroupBox("필터 (AND)")
        lay = QVBoxLayout(box)
        self.filters_table = QTableWidget(0, 3)
        self.filters_table.setHorizontalHeaderLabels(["열", "연산", "값"])
        lay.addWidget(self.filters_table)
        row = QHBoxLayout()
        add = QPushButton("＋ 필터", objectName="Ghost")
        rm = QPushButton("－ 삭제", objectName="Ghost")
        add.clicked.connect(lambda: self._add_filter_row())
        rm.clicked.connect(lambda: self._remove_row(self.filters_table))
        row.addWidget(add); row.addWidget(rm)
        lay.addLayout(row)
        return box

    def _col_combo(self):
        c = QComboBox()
        c.addItems(self.state.columns())
        return c

    def _add_rule_row(self, rule: Rule = None):
        t = self.rules_table
        r = t.rowCount()
        t.insertRow(r)
        col = self._col_combo()
        kw = QLineEdit()
        match = QComboBox(); match.addItems(["contains", "equals"])
        pr = QSpinBox(); pr.setRange(1, 3)
        color_btn = QPushButton("색")
        color_btn.setProperty("hex", "#FFFF00")
        color_btn.setStyleSheet("background:#FFFF00;color:#000;")
        color_btn.clicked.connect(lambda _, b=color_btn: self._pick_color(b))
        if rule:
            col.setCurrentText(rule.column); kw.setText(rule.keyword)
            match.setCurrentText(rule.match); pr.setValue(rule.priority)
            color_btn.setProperty("hex", rule.color)
            color_btn.setStyleSheet(f"background:{rule.color};color:#000;")
        for i, w in enumerate([col, kw, match, pr, color_btn]):
            t.setCellWidget(r, i, w)

    def _add_filter_row(self, flt: Filter = None):
        t = self.filters_table
        r = t.rowCount()
        t.insertRow(r)
        col = self._col_combo()
        op = QComboBox(); op.addItems(FILTER_OPS)
        val = QLineEdit()
        if flt:
            col.setCurrentText(flt.column); op.setCurrentText(flt.op); val.setText(flt.value)
        for i, w in enumerate([col, op, val]):
            t.setCellWidget(r, i, w)

    def _remove_row(self, table):
        r = table.currentRow()
        if r >= 0:
            table.removeRow(r)

    def _pick_color(self, btn):
        cur = QColor(btn.property("hex"))
        c = QColorDialog.getColor(cur, self, "행 색 선택")
        if c.isValid():
            btn.setProperty("hex", c.name().upper())
            btn.setStyleSheet(f"background:{c.name()};color:#000;")

    # ---- 데이터 로드 / 프리셋 ----
    def load_dataframe(self):
        cols = self.state.columns()
        self.drop_list.clear()
        for c in cols:
            it = QListWidgetItem(c)
            it.setFlags(it.flags() | Qt.ItemIsUserCheckable)
            it.setCheckState(Qt.Unchecked)
            self.drop_list.addItem(it)
        self.sort_combo.clear()
        self.sort_combo.addItems(["none", "priority"] + cols)
        self.split_combo.clear()
        self.split_combo.addItems([""] + cols)
        self._fill_preview()
        # 프리셋 값이 있으면 반영
        self.apply_preset(self.state.preset)

    def _fill_preview(self):
        df = self.state.df
        if df is None:
            return
        sample = df.head(PREVIEW_ROWS)
        self.preview.setColumnCount(len(sample.columns))
        self.preview.setHorizontalHeaderLabels([str(c) for c in sample.columns])
        self.preview.setRowCount(len(sample))
        for r in range(len(sample)):
            for c in range(len(sample.columns)):
                self.preview.setItem(r, c, QTableWidgetItem(str(sample.iat[r, c])))

    def apply_preset(self, preset):
        cols = self.state.columns()
        if not cols:
            return
        for i in range(self.drop_list.count()):
            it = self.drop_list.item(i)
            it.setCheckState(Qt.Checked if it.text() in preset.drop_columns else Qt.Unchecked)
        self.rules_table.setRowCount(0)
        for rule in preset.rules:
            self._add_rule_row(rule)
        self.filters_table.setRowCount(0)
        for flt in preset.filters:
            self._add_filter_row(flt)
        if preset.sort:
            self.sort_combo.setCurrentText(preset.sort)
        self.split_combo.setCurrentText(preset.sheet_split_column)

    def write_into(self, preset):
        preset.drop_columns = [
            self.drop_list.item(i).text()
            for i in range(self.drop_list.count())
            if self.drop_list.item(i).checkState() == Qt.Checked
        ]
        preset.rules = []
        for r in range(self.rules_table.rowCount()):
            col = self.rules_table.cellWidget(r, 0).currentText()
            kw = self.rules_table.cellWidget(r, 1).text()
            match = self.rules_table.cellWidget(r, 2).currentText()
            pr = self.rules_table.cellWidget(r, 3).value()
            color = self.rules_table.cellWidget(r, 4).property("hex")
            preset.rules.append(Rule(column=col, keyword=kw, match=match, priority=pr, color=color))
        preset.filters = []
        for r in range(self.filters_table.rowCount()):
            col = self.filters_table.cellWidget(r, 0).currentText()
            op = self.filters_table.cellWidget(r, 1).currentText()
            val = self.filters_table.cellWidget(r, 2).text()
            preset.filters.append(Filter(column=col, op=op, value=val))
        preset.sort = self.sort_combo.currentText()
        preset.sheet_split_column = self.split_combo.currentText()

    # ---- 생성 ----
    def _generate(self):
        if self.state.df is None:
            QMessageBox.warning(self, "설정", "먼저 데이터를 불러오세요.")
            return
        self.write_into(self.state.preset)
        try:
            processed = process(self.state.df, self.state.preset)
        except ValueError as e:
            QMessageBox.critical(self, "생성 실패", str(e))
            return
        fmts = date_formats(date.today())
        out = str(app_paths.output_dir() / f"{fmts['yymmdd']} 공사현장 점검 우선순위 리스트.xlsx")
        try:
            write_excel(processed, out, self.state.preset.sheet_split_column)
        except PermissionError:
            QMessageBox.critical(self, "저장 실패", "파일이 열려 있으면 닫고 다시 시도하세요.")
            return
        self.state.output_path = out
        QMessageBox.information(self, "생성 완료", f"저장됨:\n{out}")
        self.main.goto(2)
```

- [ ] **Step 2: Commit**

```bash
git add app/ui/configure_view.py
git commit -m "feat(ui): 설정 뷰(drop·규칙·필터·색·정렬·미리보기·생성) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

**참고:** 날짜는 설정 화면에서 재선택하지 않고 추출에서 고른 날짜를 파일명에 쓰는 게 이상적이나, 파일명/제목의 `{DATE}`는 "대상 날짜" 기준이어야 한다. 이를 위해 Task 10 통합에서 `AppState`에 선택 날짜를 저장하고 `_generate`/메일에서 참조하도록 배선한다(아래 Task 10 Step에 명시).

---

### Task 9: 메일 뷰 (mail_view.py)

발신자·수신자(여러 명)·제목·본문 입력 + [발송]. 생성된 엑셀을 첨부해 `MailWorker`로 전송.

**Files:**
- Create: `app/ui/mail_view.py`

**Interfaces:**
- Consumes: `AppState`, `MailWorker`, `mail_config.preset_to_mail_config`, `date_util`
- Produces:
  - `MailView(QWidget)` — `apply_preset(preset)`, `write_into(preset)`, [발송] 시 `preset_to_mail_config`로 dict 생성 후 워커 구동.

- [ ] **Step 1: 구현**

`app/ui/mail_view.py`:
```python
"""③ 메일 뷰 — 수신자·제목·본문 입력 후 발송."""

from datetime import date

from PySide6.QtWidgets import (
    QWidget, QVBoxLayout, QFormLayout, QLineEdit, QPlainTextEdit,
    QPushButton, QLabel, QMessageBox,
)

from app.core.mail_config import preset_to_mail_config
from app.core.date_util import date_formats
from app.ui.workers import MailWorker


class MailView(QWidget):
    def __init__(self, state, main):
        super().__init__()
        self.state = state
        self.main = main
        self._worker = None

        v = QVBoxLayout(self)
        v.setContentsMargins(28, 24, 28, 24)
        v.addWidget(QLabel("③ 메일 전송", objectName="H1"))
        v.addWidget(QLabel("생성된 엑셀을 첨부해 발송합니다. 제목/본문의 {DATE}는 대상 날짜로 치환됩니다.",
                           objectName="Hint"))

        form = QFormLayout()
        self.from_name = QLineEdit()
        self.from_email = QLineEdit()
        self.recipients = QPlainTextEdit()
        self.recipients.setPlaceholderText("수신자 이메일 (줄바꿈으로 여러 명)")
        self.recipients.setFixedHeight(80)
        self.subject = QLineEdit()
        self.body = QPlainTextEdit()
        form.addRow("발신자 이름", self.from_name)
        form.addRow("발신자 이메일", self.from_email)
        form.addRow("수신자", self.recipients)
        form.addRow("제목", self.subject)
        form.addRow("본문", self.body)
        v.addLayout(form)

        self.send_btn = QPushButton("메일 발송")
        self.send_btn.clicked.connect(self._send)
        v.addWidget(self.send_btn)

        self.log = QPlainTextEdit(objectName="StatusLog", readOnly=True)
        self.log.setFixedHeight(90)
        v.addWidget(self.log)

    def apply_preset(self, preset):
        self.from_name.setText(preset.mail_from_name)
        self.from_email.setText(preset.mail_from_email)
        self.recipients.setPlainText("\n".join(preset.mail_recipients))
        self.subject.setText(preset.mail_subject)
        self.body.setPlainText(preset.mail_body)

    def write_into(self, preset):
        preset.mail_from_name = self.from_name.text().strip()
        preset.mail_from_email = self.from_email.text().strip()
        preset.mail_recipients = [
            ln.strip() for ln in self.recipients.toPlainText().splitlines() if ln.strip()
        ]
        preset.mail_subject = self.subject.text()
        preset.mail_body = self.body.toPlainText()

    def _send(self):
        if not self.state.output_path:
            QMessageBox.warning(self, "메일", "먼저 ②설정에서 엑셀을 생성하세요.")
            return
        self.write_into(self.state.preset)
        mc = preset_to_mail_config(self.state.preset)
        if not mc["recipients"]:
            QMessageBox.warning(self, "메일", "수신자를 입력하세요.")
            return
        d = getattr(self.state, "target_date", None) or date.today()
        fmts = date_formats(d)
        self.send_btn.setEnabled(False)
        self.log.clear()
        self._worker = MailWorker(self.state.session, mc, self.state.output_path,
                                  fmts["yymmdd"], fmts["yy_mm_dd"])
        self._worker.status.connect(self.log.appendPlainText)
        self._worker.done.connect(self._on_done)
        self._worker.failed.connect(self._on_failed)
        self._worker.start()

    def _on_done(self, result):
        self.send_btn.setEnabled(True)
        if result.get("success"):
            self.log.appendPlainText("✅ 발송 완료")
            QMessageBox.information(self, "메일", "발송 완료!")
        else:
            msg = result.get("message", "실패")
            self.log.appendPlainText("❌ " + msg)
            QMessageBox.warning(self, "메일", f"발송 실패: {msg}\n엑셀은 저장되어 있습니다:\n{self.state.output_path}")

    def _on_failed(self, msg):
        self.send_btn.setEnabled(True)
        self.log.appendPlainText("❌ " + msg)
```

- [ ] **Step 2: Commit**

```bash
git add app/ui/mail_view.py
git commit -m "feat(ui): 메일 뷰(수신자·제목·본문·발송) 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: 진입점 + 대상 날짜 배선 (main.py)

`QApplication` 실행 진입점을 만들고 테마를 적용한다. 추출에서 고른 날짜를 `AppState.target_date`에 저장해 파일명/메일 `{DATE}`가 대상 날짜를 쓰도록 배선을 마무리한다.

**Files:**
- Create: `app/main.py`
- Modify: `app/ui/state.py` (target_date 추가), `app/ui/extract_view.py` (선택 날짜 저장), `app/ui/configure_view.py` (target_date로 파일명), `app/ui/mail_view.py` (이미 target_date 참조)

**Interfaces:**
- Produces: `app/main.py` `main()` — QApplication 생성, QSS 로드, MainWindow 표시.
- `AppState.target_date: date | None` 추가.

- [ ] **Step 1: AppState에 target_date 추가**

`app/ui/state.py`의 `__init__`에 추가:
```python
        self.target_date = None   # 추출에서 선택한 대상 날짜 (date)
```

- [ ] **Step 2: 추출 뷰에서 선택 날짜 저장**

`app/ui/extract_view.py`의 `_start`에서 `date_from` 계산 직후 추가:
```python
        from datetime import date as _date
        self.state.target_date = _date(qd.year(), qd.month(), qd.day())
```

- [ ] **Step 3: 설정 뷰의 파일명이 target_date를 쓰도록 수정**

`app/ui/configure_view.py` `_generate`에서
```python
        fmts = date_formats(date.today())
```
를
```python
        fmts = date_formats(self.state.target_date or date.today())
```
로 교체.

- [ ] **Step 4: 진입점 작성**

`app/main.py`:
```python
"""KEPCO 점검 리스트 생성기 — 진입점."""

import sys
from pathlib import Path

from PySide6.QtWidgets import QApplication

from app.ui.main_window import MainWindow


def main():
    app = QApplication(sys.argv)
    qss = Path(__file__).resolve().parent / "ui" / "theme.qss"
    if qss.exists():
        app.setStyleSheet(qss.read_text(encoding="utf-8"))
    win = MainWindow()
    win.show()
    sys.exit(app.exec())


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 임포트·기동 스모크 확인 (오프라인)**

Run: `python -c "import app.main, app.ui.main_window, app.ui.extract_view, app.ui.configure_view, app.ui.mail_view; print('import ok')"`
Expected: `import ok`

(GUI 실제 기동은 디스플레이 필요. 헤드리스 환경이면 임포트 확인으로 충분; 사내망 Windows에서 `python -m app.main`로 수동 기동 검증.)

- [ ] **Step 6: 전체 순수-로직 테스트 회귀 확인**

Run: `pytest -q`
Expected: PASS (Plan1 36 + Plan2 신규 순수테스트 전부)

- [ ] **Step 7: Commit**

```bash
git add app/main.py app/ui/state.py app/ui/extract_view.py app/ui/configure_view.py app/ui/mail_view.py
git commit -m "feat(ui): 진입점(app/main) + 대상 날짜 배선

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: 빌드 스펙 + 의존성 갱신 (build_exe.spec, requirements.txt)

진입점을 `app/main.py`로 재배선하고, 분류표.xlsx 번들을 제거하고 theme.qss를 포함한다. PySide6를 의존성에 추가한다.

**Files:**
- Modify: `build_exe.spec`, `requirements.txt`

**Interfaces:**
- Produces: `app/main.py` 진입점으로 빌드되는 단일 EXE 스펙.

- [ ] **Step 1: requirements.txt에 PySide6 추가**

`requirements.txt`의 `# Core dependencies` 블록 끝에 추가:
```
PySide6>=6.6.0
```

- [ ] **Step 2: build_exe.spec 수정**

`build_exe.spec`에서:
- `a = Analysis(['main_경남.py'], ...)` → `a = Analysis(['app/main.py'], ...)`
- `datas=[('분류표.xlsx', '.')]` → `datas=[('app/ui/theme.qss', 'app/ui')]`
- `hiddenimports=[...]`에 `'PySide6'` 추가 (기존 항목 유지)

수정 후 datas/hiddenimports 예시:
```python
    datas=[
        ('app/ui/theme.qss', 'app/ui'),  # QSS 테마 번들
    ],
    hiddenimports=[
        'PySide6',
        'websockets',
        'requests',
        'pandas',
        'openpyxl',
        'lxml',
        'xlrd',
    ],
```

- [ ] **Step 3: 스펙 파싱 확인 (오프라인)**

Run: `python -c "compile(open('build_exe.spec', encoding='utf-8').read(), 'build_exe.spec', 'exec'); print('spec ok')"`
Expected: `spec ok`

(실제 `pyinstaller build_exe.spec` 빌드는 사내망 Windows에서 수동 수행 — 아래 Task 12.)

- [ ] **Step 4: Commit**

```bash
git add build_exe.spec requirements.txt
git commit -m "build: 진입점을 app/main.py로 재배선 + PySide6 의존성/테마 번들

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 12: 수동 통합 검증 체크리스트 (문서)

사내망 Windows에서 실제 동작을 검증하는 절차를 문서화한다(자동 테스트 불가 영역).

**Files:**
- Create: `docs/manual-verification-plan2.md`

- [ ] **Step 1: 체크리스트 문서 작성**

`docs/manual-verification-plan2.md`:
```markdown
# Plan 2 수동 통합 검증 (사내망 Windows)

전제: PowerGate 실행, 사내망 연결, `pip install -r requirements.txt`.

## 기동
- [ ] `python -m app.main` 실행 → 창이 뜨고 사이드바(①②③) 표시, 테마 적용됨

## ① 추출
- [ ] 본부 드롭다운이 지역/건설/본사 그룹으로 표시, 구분줄은 선택 불가
- [ ] 날짜가 기본값(오늘+offset)으로 채워짐
- [ ] [불러오기] → 로그에 "시도 n/10" 표시, 성공 시 "완료 N행" 후 ②로 전환
- [ ] PowerGate 끄고 시도 → 재시도 후 실패 메시지 표시(크래시 없음)

## ② 설정
- [ ] 컬럼 목록/미리보기(상위 20행)가 실제 데이터로 채워짐
- [ ] 컬럼 체크 → drop 반영 / 규칙 추가(열·키워드·순위·색)·필터 추가 동작
- [ ] 색 버튼 → 색 선택기, 선택 색이 버튼에 반영
- [ ] [엑셀 생성] → 파일 저장, "생성 완료" 후 ③로 전환
- [ ] 같은 파일을 엑셀로 열어둔 채 재생성 → "파일 닫고 재시도" 안내(크래시 없음)
- [ ] 생성된 xlsx: 지사별 시트 분리, 순위 행 색, 헤더/테두리/틀고정/자동필터 확인

## ③ 메일
- [ ] 수신자 여러 명(줄바꿈) 입력, 제목/본문 {DATE} 치환 확인
- [ ] [발송] → 성공 시 완료, 실패 시 "엑셀은 저장됨" 안내

## 프리셋
- [ ] [현재 설정 저장] → 이름 입력 → presets/<이름>.json 생성
- [ ] 드롭다운에서 선택 후 [불러오기] → 본부·drop·규칙·필터·정렬·메일 전부 복원
- [ ] 다른 본부용 프리셋을 별도 저장 → 하나의 EXE로 본부 전환 확인

## 빌드
- [ ] `pyinstaller build_exe.spec` → `dist/KEPCO_RPA.exe` 생성
- [ ] EXE 실행 → 위 시나리오 반복, presets/ 폴더가 EXE 옆에 생성됨
```

- [ ] **Step 2: Commit**

```bash
git add docs/manual-verification-plan2.md
git commit -m "docs: Plan 2 수동 통합 검증 체크리스트 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review (작성자 점검 결과)

**1. 스펙 커버리지:**
- 좌측 사이드바 레이아웃 → Task 6 ✓ / 설정화면 상하 배치 → Task 8 ✓
- 날짜·본부(그룹) 선택 → Task 7 ✓ / 정렬 없이 추출 + 지사 시트 → Task 7(다운로드)·8(생성) ✓
- 컬럼 drop·범용 우선순위 규칙(열·키워드·순위·색)·필터 → Task 8 ✓
- 샘플만 미리보기(20행) → Task 8 ✓ / 프리셋 저장·불러오기(모든 설정+메일) → Task 6 ✓
- 날짜 기본값 offset → Task 2·7 ✓ / 본부 29개 하드코딩 → Plan1 departments 소비 Task 7 ✓
- 분류표.xlsx 폐기(설정=프리셋) → Task 6·8·9 ✓ / 열 스키마 이상 재시도 → Task 3·5 ✓
- QThread 비차단 + 상태 로그 → Task 5·7·9 ✓ / 재시도 루프 UI 이전 → Task 3·5 ✓
- build_exe.spec 진입점 재배선 + PySide6 → Task 11 ✓ / 수동 검증 → Task 12 ✓

**2. 플레이스홀더 스캔:** UI 뷰 4개·워커·진입점 모두 완전한 실행 코드 포함. "TBD/적절히" 없음. ✓

**3. 타입 일관성:**
- 뷰 공통 인터페이스 `apply_preset(preset)`/`write_into(preset)`를 Task 6이 호출, Task 7·8·9가 제공 — 이름 일치 ✓
- `main.configure.load_dataframe()` (Task 7 호출 → Task 8 정의) ✓
- `mailer.send_bizmail`의 `mail_config` dict를 `preset_to_mail_config`(Task 2)로 생성 → Task 9 사용 ✓
- `engine.process` ValueError 캐치 → Task 8 `_generate` ✓
- `AppState.target_date` 추가(Task 10) 후 Task 9가 참조 — Task 10에서 순서 배선 명시 ✓

**미결/후속(문서화):** Plan 1에서 이월된 Minor 중 UI가 자연 해소하는 것 — 시트명/프리셋명 충돌은 이름 입력을 UI가 통제하므로 위험 낮음(필요 시 후속). `sheet_split_column`이 `drop_columns`에 포함되면 write_excel이 단일 시트로 조용히 폴백 → Task 8에서 검증 경고를 추가하는 것이 바람직(후속 개선 항목으로 남김).
